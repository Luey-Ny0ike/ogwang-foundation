<%= form_with(model: news, local: true) do |form| %>
  <% if news.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(news.errors.count, "error") %> prohibited this news from being saved:</h2>

      <ul>
      <% news.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field col-md-8">
    <%= form.label :title %>
    <%= form.text_field :title, id: :news_title, class:"form-control" %>
  </div>

  <div class="feild col-md-8">
    <%= form.label :cover_photo %>
    <%= form.file_field :cover_photo, class:"form-control" %>
  </div>

  <div class="field col-md-8">
    <%= form.label :description %>
    <%= form.cktext_area :description, id: :news_description %>
  </div>

  <div class="actions col-md-12">
    <br><%= form.submit %>
  </div>
<% end %>
